# Generated student assets

Run `npm run assets` from `web/` to compile lecture notes and blank tutorial
sheets from the canonical LaTeX sources. Generated PDFs are deliberately not
tracked here; the deployment workflow builds them before publishing the site.

The asset builder never copies the existing instructor tutorial PDFs.
